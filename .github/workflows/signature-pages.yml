name: Build MRCH Signature v5
on:
  push:
    branches: [main]
    paths-ignore:
      - 'public/mrch-signature-v5.gif'
      - 'public/mrch-signature-v5-preview.png'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  build-signature:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install -r requirements.txt
      - run: python generate_signature_v5.py
      - name: Commit corrected Gmail assets
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -f public/mrch-signature-v5.gif public/mrch-signature-v5-preview.png
          git diff --cached --quiet || git commit -m "Publish corrected MRCH Gmail signature v5 Retina 370"
          git push
